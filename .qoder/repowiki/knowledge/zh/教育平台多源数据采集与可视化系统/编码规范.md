- 所有爬虫类均继承自 `scrapers/base.py` 中的 `BaseScraper` 抽象基类，并统一实现 `login`、`scrape` 及 `close` 等异步方法。
- 模块内广泛采用 `logging.getLogger(__name__)` 进行日志记录，并在 `web/app.py` 中统一配置了包含文件与控制台的日志处理器。
- 服务层与路由层通过 `ProgressEvent` 和订阅者模式（Pub/Sub）实现采集进度的实时广播，以支持前端 SSE（Server-Sent Events）更新。